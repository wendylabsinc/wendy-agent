{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Edge App",
      "type": "shell",
      "command": "edge run --debug --detach --agent-host edge-device.local",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}