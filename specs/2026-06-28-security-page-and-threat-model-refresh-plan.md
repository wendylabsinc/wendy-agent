# Security Page Rebuild + Threat Model Refresh — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refresh the internal threat model to reflect merged fixes, then rebuild the public security docs into a layered, CISO-convincing page (architecture + per-control status with target quarters + a public threat-model page + trust proof-points), with every claim grounded in shipped code.

**Architecture:** Three documentation deliverables in dependency order — (A) refresh `security/THREAT_MODEL.md` as the canonical source, (C) derive a public Threat Model docs page from it, (B) rebuild the public security page that links to C and surfaces a control-status table. A status-verification task runs first so every badge is backed by merged code, not Linear.

**Tech Stack:** Markdown (`THREAT_MODEL.md`), Fumadocs + Next.js MDX docs (`docs/`, nav via `meta.json`). Verification via `npm run build` in `docs/` and grep checks. No application code changes.

## Global Constraints

- Status source of truth is **merged code + git history, NOT Linear** (Linear shows shipped items as "Canceled" — e.g. WDY-1006/1008/1092).
- **No superlatives** in public copy ("most secure", "unhackable", "fully secure", "100%"). Every claim traces to shipped code or an honest status badge.
- Status badge vocabulary, used consistently everywhere: `✅ Shipped` / `🛠 In progress` / `📋 Planned`.
- **No public dates/targets.** The control-status table shows ship status only (✅/🛠/📋). Do NOT publish target quarters or months — the team has not committed public dates. No calendar references anywhere in published security docs.
- **Tracked docs source is the top-level tree `go/internal/cli/assets/docs/` (reachable as `docs/` via the repo-root symlink): e.g. `docs/security.mdx`, `docs/meta.json`, `docs/security/threat-model.mdx`.** The `docs/content/docs/` subtree is GITIGNORED and GENERATED by `docs/scripts/prepare-content.mjs` (which wipes+rebuilds it from the top-level tree) — never edit or commit there. Edits via the `docs/` symlink path work for Read/Write/Edit, but `git add`/`git rm` must use the real path `go/internal/cli/assets/docs/...` (git refuses paths "beyond a symbolic link"). Plans/specs live in repo-root `specs/`.
- Mitigations-first framing for every threat: lead with the existing control, then note hardening underway.
- Reporting email is `security@wendy.dev`; keep it.

---

### Task 1: Verify shipped-control status against merged code

Produces the authoritative status map that every later task consumes. No prose is written yet — this task only confirms what is true.

**Files:**
- Read-only: `go/internal/agent/oci/spec.go`, `oci/entitlements.go`, `oci/mounts.go`, `oci/resources.go`, `oci/namespace.go`, `go/internal/agent/containerd/client.go`
- Read-only: `specs/2026-06-27-security-issues-remediation-plan.md`, `security/THREAT_MODEL.md`
- Output: scratch note at `specs/2026-06-28-security-status-map.md`

**Interfaces:**
- Produces: `specs/2026-06-28-security-status-map.md` — a table mapping each threat ID (TM-S/T/R/I/D/E-NN) → `✅/🛠/📋` → one-line evidence (commit hash, PR number, or file:line). Tasks 2–4 read badges from this file.

- [ ] **Step 1: Confirm each known-shipped control in code**

For each row, grep/read the cited file and confirm the control exists:

```bash
cd /Users/joannisorlandos/git/wendy/wendyos
# TM-E-03 CAP_NET_ADMIN split from host networking (WDY-1094)
grep -n 'CAP_NET_ADMIN' go/internal/agent/oci/entitlements.go go/internal/agent/oci/spec.go
# TM-E-02 deny /run/containerd/* bind mounts (WDY-1102)
grep -rn 'containerd' go/internal/agent/oci/mounts.go
# TM-E-01 D-Bus hard-fail when xdg-dbus-proxy absent (WDY-1093)
grep -rn 'xdg-dbus-proxy\|dbus' go/internal/agent/oci/*.go
# TM-I-05 seccomp baseline (WDY-1012)
grep -rn 'eccomp' go/internal/agent/oci/*.go
# TM-D-02 default cpu/mem/pid limits (WDY-1101)
grep -rn 'Pids\|Memory\|CPU' go/internal/agent/oci/resources.go
# TM-T-04 user namespace / UID remap (WDY-1011) — expected NOT present yet
grep -rn 'UIDMappings\|userns\|UserNamespace' go/internal/agent/oci/namespace.go
```

Expected: first five return hits (✅ Shipped); the userns grep is the open-item check (if absent → 📋/🛠).

- [ ] **Step 2: Confirm the shipped fixes via git history**

```bash
git log --oneline --all -i --grep='WDY-1009\|WDY-1010\|WDY-1093\|WDY-1102\|WDY-1012\|WDY-1094\|WDY-1101\|WDY-1149\|WDY-1006' | head -20
```

Expected: commits present for each (e.g. `4b1ff265 fix(agent): Phase 0 security hardening (WDY-1009, WDY-1010, WDY-1093, WDY-1102)`).

- [ ] **Step 3: Write the status map**

Create `specs/2026-06-28-security-status-map.md` with a table: `Threat ID | Title | Status | Evidence`. Mark `✅ Shipped` only where Steps 1–2 confirmed code; mark open items `🛠`/`📋` per the remediation plan phases (Phase 1 signing/trust, Phase 2 isolation, Phase 3 forensics). Record the evidence (commit hash or file:line) for each ✅.

- [ ] **Step 4: Commit**

```bash
git add specs/2026-06-28-security-status-map.md
git commit -m "docs(security): verified control-status map from merged code"
```

---

### Task 2: Refresh `security/THREAT_MODEL.md`

**Files:**
- Modify: `security/THREAT_MODEL.md`
- Read-only: `specs/2026-06-28-security-status-map.md`

**Interfaces:**
- Consumes: status map from Task 1.
- Produces: refreshed `THREAT_MODEL.md` v1.1 — canonical source for Task 3's public page.

- [ ] **Step 1: Bump header and add changelog**

Change `**Version:** 1.0` → `1.1` and `**Date:** 2026-05-01` → `2026-06-28`. Add a new section after the Overview:

```markdown
## Changelog

### v1.1 — 2026-06-28
Reconciled against merged code. The following controls moved from "recommended" to
"existing mitigations": D-Bus hard-fail on missing proxy (TM-E-01), `/run/containerd/*`
mount deny (TM-E-02), `CAP_NET_ADMIN` separated from host networking (TM-E-03), baseline
seccomp profile (TM-I-05), default CPU/memory/PID limits (TM-D-02), hook command-injection
fix, debugpy loopback gating, CLI cert/org pinning, and pre-provisioning surface reduction
(TM-S-01 / TM-I-03). Open HIGH/CRITICAL items remain explicitly marked.
```

- [ ] **Step 2: Add a Status line to every threat and move shipped controls**

For each threat the status map marks `✅ Shipped`, add a `- **Status:** ✅ Mitigated` line under the severity, and move the now-implemented bullet(s) from "Recommended controls" into "Existing mitigations". For open threats add `- **Status:** 🛠 In progress` or `📋 Planned` per the map. Example for TM-E-03:

```markdown
- **Severity:** HIGH
- **Status:** ✅ Mitigated (WDY-1094)
- **Component:** Network entitlement (`host`), capabilities
...
- **Existing mitigations:** Host networking requires explicit `network: host` entitlement.
  `CAP_NET_ADMIN` is no longer granted by host networking alone; it requires a separate
  explicit entitlement.
```

- [ ] **Step 3: Re-verify the Network Ports table**

Confirm the port 50051 row reflects WDY-1006 (pre-provisioning now registers only `ProvisioningService`; other services gated behind mTLS). Update the "Auth"/"Active When" cells if the table still says all services are reachable plaintext.

- [ ] **Step 4: Update the review stamp**

In section 8, note "Last reviewed: 2026-06-28".

- [ ] **Step 5: Verify no claim overshoots the status map**

```bash
grep -n '✅ Mitigated' security/THREAT_MODEL.md
```
Expected: every `✅ Mitigated` line corresponds to a `✅ Shipped` row in `specs/2026-06-28-security-status-map.md`. Manually cross-check the list.

- [ ] **Step 6: Commit**

```bash
git add security/THREAT_MODEL.md
git commit -m "docs(security): refresh threat model to v1.1 (reconcile merged fixes)"
```

---

### Task 3: Create the public Threat Model docs page

**Files:**
- Create: `docs/security/threat-model.mdx`
- Read-only: `security/THREAT_MODEL.md`

**Interfaces:**
- Consumes: refreshed `THREAT_MODEL.md`.
- Produces: page at route `/docs/security/threat-model`, linked by Task 4.

Note: this task creates the `security/` folder. Task 4 converts the existing `security.mdx` into `security/index.mdx`, so the folder and its `meta.json` are finalized in Task 4.

- [ ] **Step 1: Write the public threat-model page**

Create `docs/security/threat-model.mdx` with frontmatter and a polished public subset of the threat model:

```mdx
---
title: Threat Model
description: WendyOS architecture, trust boundaries, and STRIDE threat analysis with current mitigation status
---

## Overview

WendyOS is threat-modeled using STRIDE across the agent, CLI, embedded registry, telemetry,
and BLE surfaces. This page summarizes the architecture, trust boundaries, and the status of
each control. We publish it because security you can audit is security you can trust.

## Architecture & Trust Boundaries

[Adapt the architecture diagram (as a fenced code block) and the TB-1..TB-6 trust-boundary
table from THREAT_MODEL.md sections 2 and 4.]

## Threat Summary (STRIDE)

[For each threat in THREAT_MODEL.md, one row: ID · category · severity · **status badge** ·
one-line mitigation-first description ("Mitigated by X" / "Hardening underway: Y, target Qn").
Lead with the control, not the gap.]
```

Pull every status badge from the refreshed `THREAT_MODEL.md` so the two never diverge.

- [ ] **Step 2: Verify no superlatives and badges are present**

```bash
grep -niE 'most secure|unhackable|fully secure|100%|impenetrable' docs/security/threat-model.mdx
grep -cE '✅|🛠|📋' docs/security/threat-model.mdx
```
Expected: first grep returns nothing; second returns a positive count.

- [ ] **Step 3: Commit** (folder is wired into nav in Task 4)

```bash
git add docs/security/threat-model.mdx
git commit -m "docs(security): add public threat model page"
```

---

### Task 4: Rebuild the public security page + wire up the security folder

**Files (real tracked paths; `docs/` symlink also works for read/write):**
- Create: `docs/security/index.mdx` (rebuilt page; real path `go/internal/cli/assets/docs/security/index.mdx`)
- Delete: `docs/security.mdx` via `git rm go/internal/cli/assets/docs/security.mdx` (replaced by the folder index)
- Create: `docs/security/meta.json` (real path `go/internal/cli/assets/docs/security/meta.json`)
- Already exists (Task 3): `docs/security/threat-model.mdx`
- Read-only: `docs/meta.json` (parent nav — entry stays `"security"`, now resolves to the folder), `specs/2026-06-28-security-status-map.md`

**Interfaces:**
- Consumes: status map (Task 1), refreshed threat model (Task 2), threat-model page (Task 3).
- Produces: route `/docs/security` (folder index preserves the existing URL) with the layered page.

- [ ] **Step 1: Convert the page to a folder index with the layered structure**

Move/rewrite the content into `docs/security/index.mdx`:

```mdx
---
title: Security
description: WendyOS security posture — architecture, control status, threat model, and disclosure
---

## Security Posture

WendyOS is a security-first edge OS: mutual TLS on every CLI↔agent and cloud connection,
post-quantum cryptography for data in transit, zero-trust certificate-based device enrollment,
sandboxed apps with explicit entitlements, and a [published threat model](/docs/security/threat-model).

## Architecture

[Trust-boundary diagram + the unprovisioned→mTLS transition + container isolation/entitlement
model + signed-update direction. Adapted from the threat model architecture section, kept short.]

## Control Status

| Control | Status |
|---------|--------|
| CLI↔agent mTLS | ✅ Shipped |
| Post-quantum data-in-transit | ✅ Shipped |
| Zero-trust cloud enrollment | ✅ Shipped |
| Container seccomp baseline | ✅ Shipped |
| CAP_NET_ADMIN gated from host networking | ✅ Shipped |
| D-Bus proxy hard-fail | ✅ Shipped |
| Default CPU/memory/PID limits | ✅ Shipped |
| CLI device/org pinning | ✅ Shipped |
| Container image signature verification | 🛠 In progress |
| Signed agent & OS updates | 🛠 In progress |
| Certificate revocation (CRL/OCSP) | 📋 Planned |
| Cloud-CA pinning in firmware | 📋 Planned |
| Container user namespaces | 📋 Planned |
| Audit logging | 📋 Planned |

[Fill every row's status from `specs/2026-06-28-security-status-map.md` + remediation phases.
No `Target` column, no dates — ship status only. The ✅ rows must each trace to a `✅ Shipped`
row in the status map.]

## Threat Model

We publish our full STRIDE threat model: [WendyOS Threat Model](/docs/security/threat-model).

## Trust & Disclosure

### Vulnerability Disclosure

Report vulnerabilities responsibly to **security@wendy.dev** rather than filing a public issue.
We acknowledge reports within [N] business days and keep you updated through remediation.

### Supply Chain

[SBOM, reproducible builds, and signed releases — present with honest status badges tied to
WDY-1001. Mark 🛠/📋 with target quarter; do NOT claim shipped.]

### Security Changelog

Recently shipped hardening:
- D-Bus proxy hard-fail when the filter proxy is absent
- `/run/containerd/*` bind mounts denied in the container spec
- `CAP_NET_ADMIN` separated from host networking
- Baseline seccomp profile for all containers
- Default CPU / memory / PID limits per container
- Hook command-injection fix; debug port bound to loopback and gated
- CLI pins device organization + cloud host on set-default

## Stay Updated

Security features are announced on our [blog](https://wendy.dev/blog) and in our
[Discord community](https://discord.gg/xYeUxq9TXv).
```

Then delete the old file:

```bash
git rm docs/security.mdx
```

- [ ] **Step 2: Add the folder meta.json**

Create `docs/security/meta.json` so the sub-pages order correctly:

```json
{
  "title": "Security",
  "pages": ["index", "threat-model"]
}
```

(The parent `meta.json` entry `"security"` now resolves to the folder; no parent edit needed. Verify in Step 4.)

- [ ] **Step 3: Verify no superlatives**

```bash
grep -niE 'most secure|unhackable|fully secure|100%|impenetrable' docs/security/index.mdx
```
Expected: nothing.

- [ ] **Step 4: Build the docs and confirm both routes resolve**

```bash
cd docs && npm run build 2>&1 | tail -30
```
Expected: build succeeds; `/docs/security` and `/docs/security/threat-model` appear in the route output with no MDX or broken-link errors.

- [ ] **Step 5: Commit**

```bash
cd /Users/joannisorlandos/git/wendy/wendyos
git add docs/security/ && git rm docs/security.mdx
git commit -m "docs(security): rebuild security page (layered, control-status, proof-points)"
```

---

### Task 5: Final review pass

**Files:**
- Read-only: all four deliverables.

- [ ] **Step 1: Cross-document consistency check**

```bash
cd /Users/joannisorlandos/git/wendy/wendyos
# Every public ✅ must match the status map
grep -oE 'TM-[STRIDE]?-?[A-Z]?-?[0-9]+' docs/security/threat-model.mdx | sort -u
# No superlatives anywhere in published security docs
grep -rniE 'most secure|unhackable|fully secure|100%|impenetrable' docs/security/
```
Expected: badges in the public page match `THREAT_MODEL.md`; no superlatives.

- [ ] **Step 2: Confirm no public dates were introduced**

```bash
grep -nE 'Q[1-4] 20[0-9]{2}' docs/security/
grep -niE 'January|February|March|April|May|June|July|August|September|October|November|December|20[0-9]{2}' docs/security/index.mdx
```
Expected: no quarter or month/year date commitments anywhere in the published security docs (the
status table is ship-status only).

## Self-Review (completed during planning)

- **Spec coverage:** Deliverable A → Task 2; B → Task 4; C → Task 3; status-grounding → Task 1; proof-points → Task 4 Step 1 (Trust & Disclosure). All spec sections mapped.
- **Placeholders:** Content steps show actual structure/copy; bracketed `[...]` notes mark where the executor adapts long source material (architecture diagram, per-threat rows) rather than hidden TODOs — each names exactly what to pull and from where.
- **Type/name consistency:** Badge vocabulary (`✅ Shipped` / `🛠 In progress` / `📋 Planned`) and the `security/index.mdx` + `security/threat-model.mdx` routes are used identically across tasks.
