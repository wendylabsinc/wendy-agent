/// Provides version information for the Edge CLI
enum Version {
    /// The current version of the CLI
    /// During development this will be "dev", but gets replaced during the build process
    static let current = "dev"
}
