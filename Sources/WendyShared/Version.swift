/// Provides version information for Wendy components
public enum Version {
    /// The current version of the software
    /// During development this will be "dev", but gets replaced during the build process
    public static let current = "dev"
}
