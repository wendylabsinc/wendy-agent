On run, Wendy builds or reuses the application image, then syncs the declared files before the app starts. The agent stores them in an app-scoped managed directory on the device and exposes them to the app as read-only runtime inputs. That gives us a clear separation: code and base dependencies stay in the image, while large or frequently edited development inputs can move through the run workflow without becoming persistent data.
