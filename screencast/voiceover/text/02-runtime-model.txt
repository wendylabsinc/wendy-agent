The mental model is simple. Declare the files once, sync them during run, and mount them read-only for the app. These are not volumes and they are not arbitrary host mounts. They are app-scoped inputs managed by Wendy, so the lifecycle is predictable and cleanup is automatic.
