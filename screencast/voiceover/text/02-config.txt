The configuration is intentionally small. Each entry has a relative source path in the project, and optionally a relative destination. If to is omitted, the destination is the same as the source path. Validation rejects absolute paths, parent directory traversal, duplicate destinations, missing sources, and source symlinks that escape the project. The app can then read the files from normal relative paths at runtime.
