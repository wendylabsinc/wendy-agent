Subsequent runs update changed inputs and remove stale entries from the managed area. Removing the app also removes these synced inputs, regardless of volume deletion flags, because they are not user data. The first implementation is deliberately scoped to the single-service run path. Multi-service and Compose flows fail explicitly for now, with follow-up issues tracking support when we design the right semantics.
