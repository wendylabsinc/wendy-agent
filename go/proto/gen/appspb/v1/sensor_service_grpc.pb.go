// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v7.35.1
// source: wendy/agent/apps/v1/sensor_service.proto

// Everything in the wendy.agent.apps package is APP-FACING: it is reachable by
// an entitled application over a per-app private unix socket that the agent
// creates for that app alone, and it is the only surface an application can
// reach. Nothing here is part of the device control plane.
//
// The control-plane counterpart is wendy.agent.services.v2, served over device
// mutual Transport Layer Security alongside provisioning, WiFi, container and
// update services. Applications cannot reach that package at all. The two live
// in separate proto packages precisely so the trust boundary is visible in the
// protocol rather than left to a comment or a runtime policy check: if a
// definition is in this package it is exposed to apps, and if it is not in this
// package it is not.

package appspbv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SensorService_Sources_FullMethodName   = "/wendy.agent.apps.v1.SensorService/Sources"
	SensorService_Subscribe_FullMethodName = "/wendy.agent.apps.v1.SensorService/Subscribe"
)

// SensorServiceClient is the client API for SensorService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SensorService is the read-only model-input surface of the harness. An app
// with the "sensor-read" entitlement receives a private socket serving THIS
// service and nothing else, so the grant cannot reach episode control,
// campaign deployment, or downloads.
//
// It is a sibling of DataService rather than two more methods on it: the
// entitlement's whole point is that the app reaches sensor subscription only.
// A gRPC server exposes services, not methods, so keeping Subscribe on
// DataService would put Start, Stop, Download, and CampaignDeploy on the same
// socket and leave a per-method interceptor allowlist as the only barrier. A
// separate service, in a separate package, makes the boundary structural
// instead of a policy check.
//
// Samples are identified by (source_id, sample_id). sample_id is assigned by
// the producer that feeds every consumer of that source, so the sample a model
// received and the sample an episode recorded carry the same identifier and
// can be joined offline. Canonical times are signed CLOCK_BOOTTIME nanoseconds.
type SensorServiceClient interface {
	// Sources lists the sensor sources this app may subscribe to, using the same
	// identifiers `wendy data sources` reports.
	Sources(ctx context.Context, in *SensorSourcesRequest, opts ...grpc.CallOption) (*SensorSourcesResponse, error)
	// Subscribe joins the existing producer for each requested source and streams
	// its samples. The subscriber is one more consumer of the same producer the
	// episode capture adapter uses, so subscribing never takes a device away from
	// capture (or from another app).
	Subscribe(ctx context.Context, in *SensorSubscribeRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[SensorSample], error)
}

type sensorServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewSensorServiceClient(cc grpc.ClientConnInterface) SensorServiceClient {
	return &sensorServiceClient{cc}
}

func (c *sensorServiceClient) Sources(ctx context.Context, in *SensorSourcesRequest, opts ...grpc.CallOption) (*SensorSourcesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SensorSourcesResponse)
	err := c.cc.Invoke(ctx, SensorService_Sources_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *sensorServiceClient) Subscribe(ctx context.Context, in *SensorSubscribeRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[SensorSample], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &SensorService_ServiceDesc.Streams[0], SensorService_Subscribe_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[SensorSubscribeRequest, SensorSample]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SensorService_SubscribeClient = grpc.ServerStreamingClient[SensorSample]

// SensorServiceServer is the server API for SensorService service.
// All implementations must embed UnimplementedSensorServiceServer
// for forward compatibility.
//
// SensorService is the read-only model-input surface of the harness. An app
// with the "sensor-read" entitlement receives a private socket serving THIS
// service and nothing else, so the grant cannot reach episode control,
// campaign deployment, or downloads.
//
// It is a sibling of DataService rather than two more methods on it: the
// entitlement's whole point is that the app reaches sensor subscription only.
// A gRPC server exposes services, not methods, so keeping Subscribe on
// DataService would put Start, Stop, Download, and CampaignDeploy on the same
// socket and leave a per-method interceptor allowlist as the only barrier. A
// separate service, in a separate package, makes the boundary structural
// instead of a policy check.
//
// Samples are identified by (source_id, sample_id). sample_id is assigned by
// the producer that feeds every consumer of that source, so the sample a model
// received and the sample an episode recorded carry the same identifier and
// can be joined offline. Canonical times are signed CLOCK_BOOTTIME nanoseconds.
type SensorServiceServer interface {
	// Sources lists the sensor sources this app may subscribe to, using the same
	// identifiers `wendy data sources` reports.
	Sources(context.Context, *SensorSourcesRequest) (*SensorSourcesResponse, error)
	// Subscribe joins the existing producer for each requested source and streams
	// its samples. The subscriber is one more consumer of the same producer the
	// episode capture adapter uses, so subscribing never takes a device away from
	// capture (or from another app).
	Subscribe(*SensorSubscribeRequest, grpc.ServerStreamingServer[SensorSample]) error
	mustEmbedUnimplementedSensorServiceServer()
}

// UnimplementedSensorServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSensorServiceServer struct{}

func (UnimplementedSensorServiceServer) Sources(context.Context, *SensorSourcesRequest) (*SensorSourcesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Sources not implemented")
}
func (UnimplementedSensorServiceServer) Subscribe(*SensorSubscribeRequest, grpc.ServerStreamingServer[SensorSample]) error {
	return status.Error(codes.Unimplemented, "method Subscribe not implemented")
}
func (UnimplementedSensorServiceServer) mustEmbedUnimplementedSensorServiceServer() {}
func (UnimplementedSensorServiceServer) testEmbeddedByValue()                       {}

// UnsafeSensorServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SensorServiceServer will
// result in compilation errors.
type UnsafeSensorServiceServer interface {
	mustEmbedUnimplementedSensorServiceServer()
}

func RegisterSensorServiceServer(s grpc.ServiceRegistrar, srv SensorServiceServer) {
	// If the following call panics, it indicates UnimplementedSensorServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SensorService_ServiceDesc, srv)
}

func _SensorService_Sources_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SensorSourcesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SensorServiceServer).Sources(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SensorService_Sources_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SensorServiceServer).Sources(ctx, req.(*SensorSourcesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _SensorService_Subscribe_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(SensorSubscribeRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(SensorServiceServer).Subscribe(m, &grpc.GenericServerStream[SensorSubscribeRequest, SensorSample]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type SensorService_SubscribeServer = grpc.ServerStreamingServer[SensorSample]

// SensorService_ServiceDesc is the grpc.ServiceDesc for SensorService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SensorService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "wendy.agent.apps.v1.SensorService",
	HandlerType: (*SensorServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Sources",
			Handler:    _SensorService_Sources_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Subscribe",
			Handler:       _SensorService_Subscribe_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "wendy/agent/apps/v1/sensor_service.proto",
}
