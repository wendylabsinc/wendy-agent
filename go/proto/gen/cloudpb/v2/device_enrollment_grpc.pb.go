// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v7.36.0
// source: wendycloud/v2/device_enrollment.proto

package cloudpbv2

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	DeviceEnrollmentService_EnrollDevice_FullMethodName = "/wendycloud.v2.DeviceEnrollmentService/EnrollDevice"
)

// DeviceEnrollmentServiceClient is the client API for DeviceEnrollmentService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// DeviceEnrollmentService turns "add a device" into a device that can actually enroll
// (WDY-2924, AAA flow 5).
//
// THE PROBLEM IT CLOSES. pki-core's ACME directory advertises externalAccountRequired, so a
// device cannot enroll without an EAB credential, and until now cloud had no surface that
// could mint one: the only paths were direct engine mTLS (CreateEABKey, deliberately not on
// FabricManagementService) or a hand-built operator-signed RelayEnrollment. Sem's ruling of
// 2026-09-06 is that no credential is hand-minted -- the automatic flow must work.
//
// Downstream of that, this is also the missing writer migration 000059 named. That migration
// added assets.pki_device_name and said in its own comment that "device enrollment against
// pki-core is not built ... Every row is NULL today, and the image-assignment query therefore
// DENIES every pull". Enrolling through this RPC is the first thing in the system that learns
// a device's pki-core name and records it, so it closes an authorization path that currently
// fails closed for the whole fleet.
//
// CLOUD IS A PURE RELAY, NEVER THE ISSUER (AAA D7). The authority is the operator's own
// signature on the enrollment request, verified by pki-core against the tenant's Operator
// Authority pool with a live revocation check. Cloud cannot sign one: its signing key is the
// grant key, and pki-core's verifier would refuse it.
//
// TWO SIGNATURES, DELIBERATELY NOT COLLAPSED. The caller signs the REQUEST (the ordinary
// x-wendy-request-signature envelope, since this is an authorization-gated mutation) and
// separately signs the enrollment request carried in `enrollment_request_jws`. Same operator,
// same key, two different scopes: reusing one for the other would either widen the request
// signature into an enrollment authority or narrow the enrollment artifact into a transport
// detail.
//
// Cloud-owned and outside the shared service-protos submodule, for the reason
// OrganizationInviteService and OrganizationProvisioningService give in their own headers:
// what crosses the fabric is pki-core's contract and lives there; this RPC is the shape of
// cloud's OWN add-device surface, which no other service participates in.
type DeviceEnrollmentServiceClient interface {
	// Registers a device and returns its enrollment credential, once.
	EnrollDevice(ctx context.Context, in *EnrollDeviceRequest, opts ...grpc.CallOption) (*EnrollDeviceResponse, error)
}

type deviceEnrollmentServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewDeviceEnrollmentServiceClient(cc grpc.ClientConnInterface) DeviceEnrollmentServiceClient {
	return &deviceEnrollmentServiceClient{cc}
}

func (c *deviceEnrollmentServiceClient) EnrollDevice(ctx context.Context, in *EnrollDeviceRequest, opts ...grpc.CallOption) (*EnrollDeviceResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(EnrollDeviceResponse)
	err := c.cc.Invoke(ctx, DeviceEnrollmentService_EnrollDevice_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// DeviceEnrollmentServiceServer is the server API for DeviceEnrollmentService service.
// All implementations must embed UnimplementedDeviceEnrollmentServiceServer
// for forward compatibility.
//
// DeviceEnrollmentService turns "add a device" into a device that can actually enroll
// (WDY-2924, AAA flow 5).
//
// THE PROBLEM IT CLOSES. pki-core's ACME directory advertises externalAccountRequired, so a
// device cannot enroll without an EAB credential, and until now cloud had no surface that
// could mint one: the only paths were direct engine mTLS (CreateEABKey, deliberately not on
// FabricManagementService) or a hand-built operator-signed RelayEnrollment. Sem's ruling of
// 2026-09-06 is that no credential is hand-minted -- the automatic flow must work.
//
// Downstream of that, this is also the missing writer migration 000059 named. That migration
// added assets.pki_device_name and said in its own comment that "device enrollment against
// pki-core is not built ... Every row is NULL today, and the image-assignment query therefore
// DENIES every pull". Enrolling through this RPC is the first thing in the system that learns
// a device's pki-core name and records it, so it closes an authorization path that currently
// fails closed for the whole fleet.
//
// CLOUD IS A PURE RELAY, NEVER THE ISSUER (AAA D7). The authority is the operator's own
// signature on the enrollment request, verified by pki-core against the tenant's Operator
// Authority pool with a live revocation check. Cloud cannot sign one: its signing key is the
// grant key, and pki-core's verifier would refuse it.
//
// TWO SIGNATURES, DELIBERATELY NOT COLLAPSED. The caller signs the REQUEST (the ordinary
// x-wendy-request-signature envelope, since this is an authorization-gated mutation) and
// separately signs the enrollment request carried in `enrollment_request_jws`. Same operator,
// same key, two different scopes: reusing one for the other would either widen the request
// signature into an enrollment authority or narrow the enrollment artifact into a transport
// detail.
//
// Cloud-owned and outside the shared service-protos submodule, for the reason
// OrganizationInviteService and OrganizationProvisioningService give in their own headers:
// what crosses the fabric is pki-core's contract and lives there; this RPC is the shape of
// cloud's OWN add-device surface, which no other service participates in.
type DeviceEnrollmentServiceServer interface {
	// Registers a device and returns its enrollment credential, once.
	EnrollDevice(context.Context, *EnrollDeviceRequest) (*EnrollDeviceResponse, error)
	mustEmbedUnimplementedDeviceEnrollmentServiceServer()
}

// UnimplementedDeviceEnrollmentServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedDeviceEnrollmentServiceServer struct{}

func (UnimplementedDeviceEnrollmentServiceServer) EnrollDevice(context.Context, *EnrollDeviceRequest) (*EnrollDeviceResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method EnrollDevice not implemented")
}
func (UnimplementedDeviceEnrollmentServiceServer) mustEmbedUnimplementedDeviceEnrollmentServiceServer() {
}
func (UnimplementedDeviceEnrollmentServiceServer) testEmbeddedByValue() {}

// UnsafeDeviceEnrollmentServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to DeviceEnrollmentServiceServer will
// result in compilation errors.
type UnsafeDeviceEnrollmentServiceServer interface {
	mustEmbedUnimplementedDeviceEnrollmentServiceServer()
}

func RegisterDeviceEnrollmentServiceServer(s grpc.ServiceRegistrar, srv DeviceEnrollmentServiceServer) {
	// If the following call panics, it indicates UnimplementedDeviceEnrollmentServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&DeviceEnrollmentService_ServiceDesc, srv)
}

func _DeviceEnrollmentService_EnrollDevice_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(EnrollDeviceRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(DeviceEnrollmentServiceServer).EnrollDevice(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: DeviceEnrollmentService_EnrollDevice_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(DeviceEnrollmentServiceServer).EnrollDevice(ctx, req.(*EnrollDeviceRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// DeviceEnrollmentService_ServiceDesc is the grpc.ServiceDesc for DeviceEnrollmentService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var DeviceEnrollmentService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "wendycloud.v2.DeviceEnrollmentService",
	HandlerType: (*DeviceEnrollmentServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "EnrollDevice",
			Handler:    _DeviceEnrollmentService_EnrollDevice_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "wendycloud/v2/device_enrollment.proto",
}
