// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v7.36.0
// source: cloud/data_ingest.proto

package cloudpb

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	DataIngestService_BeginEpisodeUpload_FullMethodName = "/wendycloud.data.v1.DataIngestService/BeginEpisodeUpload"
	DataIngestService_UploadEpisodeChunk_FullMethodName = "/wendycloud.data.v1.DataIngestService/UploadEpisodeChunk"
	DataIngestService_CommitEpisode_FullMethodName      = "/wendycloud.data.v1.DataIngestService/CommitEpisode"
	DataIngestService_QueryEpisodes_FullMethodName      = "/wendycloud.data.v1.DataIngestService/QueryEpisodes"
	DataIngestService_GetEpisode_FullMethodName         = "/wendycloud.data.v1.DataIngestService/GetEpisode"
)

// DataIngestServiceClient is the client API for DataIngestService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// DataIngestService receives sealed episode captures from device transfer
// workers (the WendyOS "Wendy Data" capture engine) and serves the episode
// catalog to readers (dashboard, BI, training pipelines).
//
// Upload protocol (mirrors the device DataService chunked Download in
// reverse):
//  1. BeginEpisodeUpload(manifest) registers the episode in the catalog as
//     `uploading` and returns per-file committed offsets. Idempotent: safe
//     to call on every transfer-worker retry; already-stored files report
//     their full size so the worker skips them.
//  2. UploadEpisodeChunk streams file chunks (at most 1 MiB of data per
//     chunk, sequential offsets per file, eof on the last chunk). Each
//     chunk is acknowledged with the received (spooled) offset and the
//     committed (durably stored in object storage) offset. An ack names the
//     (episode_id, path) it belongs to and must be matched on that pair,
//     because one stream may carry chunks for more than one episode and a
//     path is unique only within an episode.
//  3. CommitEpisode verifies the SHA-256 of every stored object against
//     the manifest and flips the catalog state to `complete`, or `failed`
//     with per-file detail on any mismatch.
//
// Authentication:
//   - Begin/Upload/Commit require a device asset identity (mTLS client
//     certificate SAN). The owning organization and asset are read from that
//     certificate and attested by the cloud; they are not carried on the
//     request. Every catalog row and every storage object name is written
//     from the certificate identity, so an upload cannot name a tenant it
//     does not hold a certificate for.
//   - QueryEpisodes/GetEpisode are read RPCs for users and services; the
//     serving implementation defines the accepted credential. Both carry an
//     org_id whose membership the server does not verify today, and the read
//     credential in use is a single shared token scoped to no organization.
//     GetEpisode returns signed object URLs, so that combination is a
//     cross-organization data-egress path, not just a catalog read. Read the
//     org_id documentation on QueryEpisodesRequest and GetEpisodeRequest
//     before exposing either RPC beyond a trusted internal caller.
type DataIngestServiceClient interface {
	// Registers (or re-registers) an episode upload. Idempotent.
	BeginEpisodeUpload(ctx context.Context, in *BeginEpisodeUploadRequest, opts ...grpc.CallOption) (*BeginEpisodeUploadResponse, error)
	// Streams file chunks for an episode registered via BeginEpisodeUpload.
	// Chunks for one file must arrive in offset order; files may be
	// interleaved. Every chunk is acknowledged in order.
	UploadEpisodeChunk(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[EpisodeChunk, EpisodeChunkAck], error)
	// Verifies stored objects against the manifest checksums and finalizes
	// the episode.
	CommitEpisode(ctx context.Context, in *CommitEpisodeRequest, opts ...grpc.CallOption) (*CommitEpisodeResponse, error)
	// Catalog queries. Episodes are returned with per-file retrieval URLs
	// (short-lived signed object-storage URLs).
	QueryEpisodes(ctx context.Context, in *QueryEpisodesRequest, opts ...grpc.CallOption) (*QueryEpisodesResponse, error)
	GetEpisode(ctx context.Context, in *GetEpisodeRequest, opts ...grpc.CallOption) (*Episode, error)
}

type dataIngestServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewDataIngestServiceClient(cc grpc.ClientConnInterface) DataIngestServiceClient {
	return &dataIngestServiceClient{cc}
}

func (c *dataIngestServiceClient) BeginEpisodeUpload(ctx context.Context, in *BeginEpisodeUploadRequest, opts ...grpc.CallOption) (*BeginEpisodeUploadResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(BeginEpisodeUploadResponse)
	err := c.cc.Invoke(ctx, DataIngestService_BeginEpisodeUpload_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *dataIngestServiceClient) UploadEpisodeChunk(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[EpisodeChunk, EpisodeChunkAck], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &DataIngestService_ServiceDesc.Streams[0], DataIngestService_UploadEpisodeChunk_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[EpisodeChunk, EpisodeChunkAck]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type DataIngestService_UploadEpisodeChunkClient = grpc.BidiStreamingClient[EpisodeChunk, EpisodeChunkAck]

func (c *dataIngestServiceClient) CommitEpisode(ctx context.Context, in *CommitEpisodeRequest, opts ...grpc.CallOption) (*CommitEpisodeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CommitEpisodeResponse)
	err := c.cc.Invoke(ctx, DataIngestService_CommitEpisode_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *dataIngestServiceClient) QueryEpisodes(ctx context.Context, in *QueryEpisodesRequest, opts ...grpc.CallOption) (*QueryEpisodesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(QueryEpisodesResponse)
	err := c.cc.Invoke(ctx, DataIngestService_QueryEpisodes_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *dataIngestServiceClient) GetEpisode(ctx context.Context, in *GetEpisodeRequest, opts ...grpc.CallOption) (*Episode, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Episode)
	err := c.cc.Invoke(ctx, DataIngestService_GetEpisode_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// DataIngestServiceServer is the server API for DataIngestService service.
// All implementations must embed UnimplementedDataIngestServiceServer
// for forward compatibility.
//
// DataIngestService receives sealed episode captures from device transfer
// workers (the WendyOS "Wendy Data" capture engine) and serves the episode
// catalog to readers (dashboard, BI, training pipelines).
//
// Upload protocol (mirrors the device DataService chunked Download in
// reverse):
//  1. BeginEpisodeUpload(manifest) registers the episode in the catalog as
//     `uploading` and returns per-file committed offsets. Idempotent: safe
//     to call on every transfer-worker retry; already-stored files report
//     their full size so the worker skips them.
//  2. UploadEpisodeChunk streams file chunks (at most 1 MiB of data per
//     chunk, sequential offsets per file, eof on the last chunk). Each
//     chunk is acknowledged with the received (spooled) offset and the
//     committed (durably stored in object storage) offset. An ack names the
//     (episode_id, path) it belongs to and must be matched on that pair,
//     because one stream may carry chunks for more than one episode and a
//     path is unique only within an episode.
//  3. CommitEpisode verifies the SHA-256 of every stored object against
//     the manifest and flips the catalog state to `complete`, or `failed`
//     with per-file detail on any mismatch.
//
// Authentication:
//   - Begin/Upload/Commit require a device asset identity (mTLS client
//     certificate SAN). The owning organization and asset are read from that
//     certificate and attested by the cloud; they are not carried on the
//     request. Every catalog row and every storage object name is written
//     from the certificate identity, so an upload cannot name a tenant it
//     does not hold a certificate for.
//   - QueryEpisodes/GetEpisode are read RPCs for users and services; the
//     serving implementation defines the accepted credential. Both carry an
//     org_id whose membership the server does not verify today, and the read
//     credential in use is a single shared token scoped to no organization.
//     GetEpisode returns signed object URLs, so that combination is a
//     cross-organization data-egress path, not just a catalog read. Read the
//     org_id documentation on QueryEpisodesRequest and GetEpisodeRequest
//     before exposing either RPC beyond a trusted internal caller.
type DataIngestServiceServer interface {
	// Registers (or re-registers) an episode upload. Idempotent.
	BeginEpisodeUpload(context.Context, *BeginEpisodeUploadRequest) (*BeginEpisodeUploadResponse, error)
	// Streams file chunks for an episode registered via BeginEpisodeUpload.
	// Chunks for one file must arrive in offset order; files may be
	// interleaved. Every chunk is acknowledged in order.
	UploadEpisodeChunk(grpc.BidiStreamingServer[EpisodeChunk, EpisodeChunkAck]) error
	// Verifies stored objects against the manifest checksums and finalizes
	// the episode.
	CommitEpisode(context.Context, *CommitEpisodeRequest) (*CommitEpisodeResponse, error)
	// Catalog queries. Episodes are returned with per-file retrieval URLs
	// (short-lived signed object-storage URLs).
	QueryEpisodes(context.Context, *QueryEpisodesRequest) (*QueryEpisodesResponse, error)
	GetEpisode(context.Context, *GetEpisodeRequest) (*Episode, error)
	mustEmbedUnimplementedDataIngestServiceServer()
}

// UnimplementedDataIngestServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedDataIngestServiceServer struct{}

func (UnimplementedDataIngestServiceServer) BeginEpisodeUpload(context.Context, *BeginEpisodeUploadRequest) (*BeginEpisodeUploadResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method BeginEpisodeUpload not implemented")
}
func (UnimplementedDataIngestServiceServer) UploadEpisodeChunk(grpc.BidiStreamingServer[EpisodeChunk, EpisodeChunkAck]) error {
	return status.Error(codes.Unimplemented, "method UploadEpisodeChunk not implemented")
}
func (UnimplementedDataIngestServiceServer) CommitEpisode(context.Context, *CommitEpisodeRequest) (*CommitEpisodeResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method CommitEpisode not implemented")
}
func (UnimplementedDataIngestServiceServer) QueryEpisodes(context.Context, *QueryEpisodesRequest) (*QueryEpisodesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method QueryEpisodes not implemented")
}
func (UnimplementedDataIngestServiceServer) GetEpisode(context.Context, *GetEpisodeRequest) (*Episode, error) {
	return nil, status.Error(codes.Unimplemented, "method GetEpisode not implemented")
}
func (UnimplementedDataIngestServiceServer) mustEmbedUnimplementedDataIngestServiceServer() {}
func (UnimplementedDataIngestServiceServer) testEmbeddedByValue()                           {}

// UnsafeDataIngestServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to DataIngestServiceServer will
// result in compilation errors.
type UnsafeDataIngestServiceServer interface {
	mustEmbedUnimplementedDataIngestServiceServer()
}

func RegisterDataIngestServiceServer(s grpc.ServiceRegistrar, srv DataIngestServiceServer) {
	// If the following call panics, it indicates UnimplementedDataIngestServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&DataIngestService_ServiceDesc, srv)
}

func _DataIngestService_BeginEpisodeUpload_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(BeginEpisodeUploadRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(DataIngestServiceServer).BeginEpisodeUpload(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: DataIngestService_BeginEpisodeUpload_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(DataIngestServiceServer).BeginEpisodeUpload(ctx, req.(*BeginEpisodeUploadRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _DataIngestService_UploadEpisodeChunk_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(DataIngestServiceServer).UploadEpisodeChunk(&grpc.GenericServerStream[EpisodeChunk, EpisodeChunkAck]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type DataIngestService_UploadEpisodeChunkServer = grpc.BidiStreamingServer[EpisodeChunk, EpisodeChunkAck]

func _DataIngestService_CommitEpisode_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CommitEpisodeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(DataIngestServiceServer).CommitEpisode(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: DataIngestService_CommitEpisode_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(DataIngestServiceServer).CommitEpisode(ctx, req.(*CommitEpisodeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _DataIngestService_QueryEpisodes_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(QueryEpisodesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(DataIngestServiceServer).QueryEpisodes(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: DataIngestService_QueryEpisodes_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(DataIngestServiceServer).QueryEpisodes(ctx, req.(*QueryEpisodesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _DataIngestService_GetEpisode_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetEpisodeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(DataIngestServiceServer).GetEpisode(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: DataIngestService_GetEpisode_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(DataIngestServiceServer).GetEpisode(ctx, req.(*GetEpisodeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// DataIngestService_ServiceDesc is the grpc.ServiceDesc for DataIngestService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var DataIngestService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "wendycloud.data.v1.DataIngestService",
	HandlerType: (*DataIngestServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "BeginEpisodeUpload",
			Handler:    _DataIngestService_BeginEpisodeUpload_Handler,
		},
		{
			MethodName: "CommitEpisode",
			Handler:    _DataIngestService_CommitEpisode_Handler,
		},
		{
			MethodName: "QueryEpisodes",
			Handler:    _DataIngestService_QueryEpisodes_Handler,
		},
		{
			MethodName: "GetEpisode",
			Handler:    _DataIngestService_GetEpisode_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "UploadEpisodeChunk",
			Handler:       _DataIngestService_UploadEpisodeChunk_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "cloud/data_ingest.proto",
}
