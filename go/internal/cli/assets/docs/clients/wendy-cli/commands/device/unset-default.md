Unsets the [default device](../../device-selection.md), so that other commands will prompt selection again.

Use [`get-default`](./get-default.md) to see the current default, or [`set-default`](./set-default.md) to set a new one.