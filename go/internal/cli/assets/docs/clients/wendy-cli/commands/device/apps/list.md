Lists all uploaded apps for a given device.

Shows the app's status (running), the app ID and app version.
If available, also shows the RAM usage and disk space.

You can also, through this menu, interactively start, stop and remove apps - or view an app's logs.