`wendy project entitlements remove` can be run with an entitlement as input, or without.

`wendy project entitlements remove <entitlement>` will immediately update `wendy.json` to remove the entitlement.

Omitting the entitlement from the invocation shows a list of all active entitlements and an interactive menu to remove them.