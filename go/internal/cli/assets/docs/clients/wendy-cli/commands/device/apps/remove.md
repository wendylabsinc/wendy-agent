Removes an app from the device. If an `[app-name]` argument is provided, removes that app. Otherwise, lists all apps and allows interactive removal.

- `--cleanup`: is provided, the container is removed too - reclaiming disk space.
- `--delete-volumes` deletes persistent-volumes
- `--force`, forces deletion without confirmation
